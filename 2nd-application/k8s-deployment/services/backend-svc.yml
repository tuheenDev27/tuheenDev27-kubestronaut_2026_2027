apiVersion: v1
kind: Service
metadata:
  name: application-backend
  labels:
    app: 2nd-application-backend
    tier: backend
spec:
  type: NodePort
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30002
  selector:
    app: application-backend