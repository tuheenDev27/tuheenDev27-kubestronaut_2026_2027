apiVersion: v1
kind: Service
metadata:
  name: application-backend
  labels:
    app: 2nd-application-backend
    tier: backend
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30002
  selector:
    app: 2nd-application-backend