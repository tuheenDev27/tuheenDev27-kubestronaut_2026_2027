apiVersion: v1
kind: Pod
metadata:
  name: mongodb-pod
  labels:
    # This label connects the Pod to the Service above
    app: database 
spec:
  containers:
  - name: mongodb
    image: mongo:latest
    ports:
    - containerPort: 27017